services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      BACKUP_DESTINATION_LABEL: "Google Drive (G:\\Il mio Drive\\FTC_HUB_Backup)"
      SMTP_HOST: smtp.aruba.it
      SMTP_PORT: "465"
      SMTP_USER: "ftc_hub@flyingtigeritalia.com"
      SMTP_PASSWORD: "2CandidoCannavo!"
      SMTP_FROM: "ftc_hub@flyingtigeritalia.com"
    volumes:
      - ./backend:/app
      - attachments_data:/data/attachments
      - D:/:/mnt/f
      - D:/FTC_HUB_Backup:/mnt/backup
      # Vedi nota nel base: il router agent-installer legge da /navision_agent
      # e /nav_agent (nuovo agente Python)
      - ./navision_agent:/navision_agent:ro
      - ./nav_agent:/nav_agent:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    # In prod il frontend e' buildato staticamente (nginx statico).
    # Le parti dev-only (command, volumes, environment) vivono in
    # docker-compose.override.yml che NON viene letto in prod perche'
    # qui passiamo -f espliciti che disabilitano il merge automatico.
